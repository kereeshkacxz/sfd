version: '3'

services:
  sfd-app:
    build: .
    container_name: sfd_container
    ports:
      - "8000:8000"  # Порт для бэкенда
      - "3000:3000"  # Порт для фронтенда
    restart: unless-stopped
    volumes:
      - git-update-logs:/app/git-update.log

volumes:
  git-update-logs:
